app:
  config:
    id: 'my-app'

llm:
  provider: openai
  config:
    model: 'gpt-3.5-turbo-1106'
    temperature: 0.5
    max_tokens: 1000
    top_p: 1
    stream: false

embedder:
  provider: openai
  config:
    model: 'text-embedding-ada-002'
